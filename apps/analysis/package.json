{
  "name": "@eqstack/analysis",
  "version": "0.0.0",
  "private": true,
  "description": "Placeholder shell for the EQStack relationship-analysis app. No domain code yet — proves the monorepo structure end-to-end (build/typecheck/lint/test under turbo). Design starts once the analytics research lands (HANDOFF.md Q2).",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@eqstack/biome-config": "workspace:*",
    "@eqstack/tsconfig": "workspace:*",
    "@eqstack/vitest-config": "workspace:*",
    "@types/node": "^24.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.2.7"
  },
  "engines": {
    "node": ">=24"
  }
}
